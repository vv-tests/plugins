import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sinan.fblogin',
  appName: 'plugs',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
